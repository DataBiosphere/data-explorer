runtime: custom
env: flex
service: api
entrypoint: gunicorn -b ":$PORT" data_explorer.__main__:app

env_variables:
  PATH_PREFIX: /api
  ELASTICSEARCH_URL: http://10.128.0.9:9200/
  DATASET_CONFIG_DIR: dataset_config/1000_genomes
